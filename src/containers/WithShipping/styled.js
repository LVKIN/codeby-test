import styled from 'styled-components';
import { rem } from 'polished';

export const WithShippingCart = styled.section`
  width: ${rem(375)};
  background-color: #ffffff;
  border-radius: ${rem(20)};
  height: fit-content;
`;
