import styled from 'styled-components';
import { rem } from 'polished';

export const WithoutShippingCart = styled.section`
  width: ${rem(375)};
  background-color: #ffffff;
  border-radius: ${rem(20)};
  margin-bottom: ${rem(120)};
`;
